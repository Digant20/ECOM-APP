import React from 'react';
import cssClass from './Spinner.module.css';

const Spinner = () => {
    return (
        
            <div class={cssClass.Spinner}>Loading...</div>
        
    );
}

export default Spinner;
